<div class="row">
  <div class="col-4">
    <h4>Happy kitchen consumers</h4>
    <p>Who wants to eat?</p>
    <ul id="consumers">
      <%= render @consumers %>
    </ul>

    <%= form_with(model: @consumer, class: "form-inline") do |f| %>
      <%= f.label :name, "Name:", class: "mr-sm-2" %><br>
      <%= f.text_field :name, required: true, class: "form-control form-control-sm mb-2 mr-sm-2" %>
      <%= f.submit class: "btn btn-primary btn-sm mb-2" %>
    <% end %>
  </div>
  <div class="col-8">
    <h4 class="text-center">Weekly bills</h4>
    <table class="table table-sm">
      <thead>
        <tr>
          <th scope="col">Name</th>
          <th scope="col" class="text-center">Monday</th>
          <th scope="col" class="text-center">Tuesday</th>
          <th scope="col" class="text-center">Wednesday</th>
          <th scope="col" class="text-center">Thursday</th>
        </tr>
      </thead>
      <tbody>
        <tr class="text-info">
          <td>Shipping per person</td>
          <td class="text-center"><%= DailyOffer.for_day(current_monday)&.first&.shipping_price_per_person %></td>
          <td class="text-center"><%= DailyOffer.for_day(current_monday + 1.day)&.first&.shipping_price_per_person %></td>
          <td class="text-center"><%= DailyOffer.for_day(current_monday + 2.days)&.first&.shipping_price_per_person %></td>
          <td class="text-center"><%= DailyOffer.for_day(current_monday + 3.days)&.first&.shipping_price_per_person %></td>
        </tr>
        <% @consumers.each do |consumer| %>
          <tr>
            <td><%= consumer.name %></td>
            <td class="text-center"><%= consumer.daily_bill(current_monday) %></td>
            <td class="text-center"><%= consumer.daily_bill(current_monday + 1.day) %></td>
            <td class="text-center"><%= consumer.daily_bill(current_monday + 2.days) %></td>
            <td class="text-center"><%= consumer.daily_bill(current_monday + 3.days) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
